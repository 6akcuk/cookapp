<div class="clearfix">
  <span class="pull-left admin-list-counter-header">{{ $header }} <span class="admin-list-counter">{{ $total }}</span></span>
  <a href="{{ route($createRoute) }}" class="btn btn-primary pull-right">{{ $createButtonText }}</a>
</div>
